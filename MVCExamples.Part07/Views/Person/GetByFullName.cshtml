
@{
    ViewData["Title"] = "GetByFullName";
}

<h1>GetByFullName</h1>

@{
    var info = ViewBag.ID + " " + ViewBag.Name + " " + ViewBag.Family;
}

<h2 class="alert-info">Person Full Information = @info</h2>